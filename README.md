# CS 1.6 HLSDK WebSocket Hook & Logging System

Комплексный проект для захвата игровых данных из Counter-Strike 1.6 (Steam версия) и передачи их через WebSocket в реальном времени.

## Описание проекта

Этот проект состоит из двух основных компонентов:

1. **DLL модуль (C++)** - захватывает данные из памяти CS 1.6 и отправляет через WebSocket
2. **Web интерфейс (Next.js)** - принимает и отображает данные в реальном времени

## Возможности

### Захват данных игры:
- ✅ Имена всех игроков в матче
- ✅ K/D/A статистика (Kills, Deaths, Assists)
- ✅ Количество денег у игроков
- ✅ Статус бомбы (установлена, таймер, обезврежена)
- ✅ Игровые события (Bomb Planted, Defused, Round Start/End)
- ✅ Детальное логирование для отладки

### Технические особенности:
- 🔒 Безопасное чтение памяти с проверкой валидности адресов
- 🔄 Автоматическое переподключение WebSocket при разрыве связи
- ⚡ Асинхронная отправка данных (минимальное влияние на FPS)
- 📝 Подробное логирование с timestamp'ами
- 🎯 Использование HLSDK для работы с CS 1.6

## Структура проекта

```
.
├── dll/                    # C++ DLL модуль для CS 1.6
│   ├── include/           # Заголовочные файлы
│   ├── src/               # Исходный код
│   ├── examples/          # Примеры использования
│   ├── CMakeLists.txt     # Конфигурация сборки
│   └── README.md          # Документация DLL
│
├── app/                   # Next.js web приложение
├── public/                # Статические файлы
└── package.json           # Node.js зависимости
```

## Быстрый старт

### 1. DLL модуль (Windows)

Подробные инструкции по сборке и использованию DLL находятся в [dll/README.md](dll/README.md)

Краткая инструкция:

```bash
cd dll
mkdir build && cd build
cmake ..
cmake --build . --config Release
```

DLL файл будет находиться в `dll/build/bin/Release/cs16_datacapture.dll`

### 2. Web сервер (Node.js)

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 3. Тестовый WebSocket сервер

Для тестирования DLL используйте готовый тестовый сервер:

```bash
node dll/examples/test_websocket_server.js
```

Сервер будет слушать на порту 8080 и выводить все получаемые данные из игры.

## Использование

### Шаг 1: Запустите WebSocket сервер

```bash
node dll/examples/test_websocket_server.js
```

### Шаг 2: Запустите Counter-Strike 1.6

Запустите игру и подключитесь к любому серверу (рекомендуется локальный для тестирования).

### Шаг 3: Инжектируйте DLL

Используйте любой DLL инжектор (например, Extreme Injector):
1. Выберите процесс `hl.exe`
2. Выберите файл `cs16_datacapture.dll`
3. Нажмите "Inject"

DLL автоматически начнет захватывать и отправлять данные.

### Шаг 4: Наблюдайте за данными

Проверьте:
- Вывод тестового сервера WebSocket
- Лог файл `cs16_datacapture.log` рядом с `hl.exe`
- Web интерфейс (если настроен)

## Формат данных

DLL отправляет данные в JSON формате:

```json
{
  "players": [
    {
      "name": "Player1",
      "kills": 10,
      "deaths": 5,
      "assists": 2,
      "money": 3200,
      "team": 2,
      "isAlive": true
    }
  ],
  "bomb": {
    "planted": true,
    "timeRemaining": 35.5,
    "defused": false
  },
  "events": ["Bomb Planted"],
  "roundNumber": 5,
  "roundTime": 120.5
}
```

## Конфигурация

### DLL настройки (dll/src/dllmain.cpp):

```cpp
static const std::string WS_HOST = "localhost";  // WebSocket сервер
static const int WS_PORT = 8080;                 // Порт
static const std::string LOG_FILE = "cs16_datacapture.log";  // Лог файл
```

### Web сервер настройки:

Настройки можно изменить в соответствующих конфигурационных файлах Next.js.

## Важные замечания

### ⚠️ Безопасность и VAC

**ВНИМАНИЕ**: Использование данной DLL на серверах с VAC (Valve Anti-Cheat) может привести к бану. Используйте ТОЛЬКО:
- На локальных серверах
- На серверах без VAC защиты
- На свой страх и риск

### 📝 Смещения памяти

Для корректной работы необходимо настроить смещения памяти для вашей версии CS 1.6. См. подробное руководство: [dll/MEMORY_OFFSETS_GUIDE.md](dll/MEMORY_OFFSETS_GUIDE.md)

Используйте инструменты:
- Cheat Engine - для поиска адресов
- IDA Pro / Ghidra - для реверс-инжиниринга
- x64dbg - для отладки

## Документация

- [DLL модуль - README](dll/README.md)
- [Руководство по поиску смещений памяти](dll/MEMORY_OFFSETS_GUIDE.md)
- [Примеры использования](dll/examples/)

## Разработка

### Требования:

**Для DLL (C++):**
- Windows 10/11
- Visual Studio 2019+ или CMake 3.15+
- Windows SDK
- C++17 компилятор

**Для Web (Node.js):**
- Node.js 18+
- npm или yarn

### Сборка из исходников:

```bash
# DLL модуль
cd dll/build
cmake .. -G "Visual Studio 16 2019"
cmake --build . --config Release

# Web сервер
npm install
npm run build
```

## Тестирование

### Тест DLL загрузчика:

```bash
cd dll/examples
# Компиляция и запуск тестового загрузчика
```

### Тест WebSocket сервера:

```bash
node dll/examples/test_websocket_server.js
```

## Troubleshooting

### DLL не загружается
- Проверьте, что DLL собрана для x86 архитектуры (CS 1.6 - 32-bit)
- Убедитесь, что все зависимости доступны
- Проверьте логи в `cs16_datacapture.log`

### Нет данных от игры
- Смещения памяти нуждаются в обновлении
- Используйте Cheat Engine для поиска актуальных адресов
- См. [MEMORY_OFFSETS_GUIDE.md](dll/MEMORY_OFFSETS_GUIDE.md)

### WebSocket не подключается
- Убедитесь, что сервер запущен на порту 8080
- Проверьте настройки файрвола
- Проверьте логи подключения

## Roadmap

- [ ] Автоматическое определение смещений памяти
- [ ] Поддержка разных версий CS 1.6
- [ ] Расширенная аналитика игровых данных
- [ ] Real-time графики и статистика в web интерфейсе
- [ ] Поддержка нескольких игровых серверов одновременно
- [ ] Запись и воспроизведение игровых сессий
- [ ] API для интеграции с другими приложениями

## Технологии

- **C++17** - основной язык DLL модуля
- **CMake** - система сборки
- **WinAPI** - работа с памятью процесса
- **TCP Sockets** - коммуникация через WebSocket
- **Next.js** - web фреймворк
- **TypeScript** - типизированный JavaScript
- **React** - UI библиотека

## Лицензия

MIT License - свободно используйте и модифицируйте.

## Контакты и поддержка

При возникновении проблем:
1. Проверьте [Issues](https://github.com/your-repo/issues)
2. Прочитайте документацию в папке `dll/`
3. Проверьте логи `cs16_datacapture.log`

## Авторы

Разработано как учебный/исследовательский проект.

---

**Дисклеймер**: Этот проект создан исключительно в образовательных целях. Авторы не несут ответственности за любое использование кода, которое нарушает правила игры или сервисов. Используйте на свой страх и риск.
