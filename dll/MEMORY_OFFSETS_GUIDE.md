# Руководство по поиску смещений памяти для CS 1.6

Данный документ описывает процесс поиска необходимых смещений памяти для корректной работы DLL.

## Необходимые инструменты

1. **Cheat Engine** - основной инструмент для поиска адресов в памяти
   - Скачать: https://www.cheatengine.org/
   
2. **IDA Pro / Ghidra** - для более глубокого анализа (опционально)
   - IDA Pro: https://hex-rays.com/ida-pro/
   - Ghidra: https://ghidra-sre.org/

3. **x64dbg** - отладчик для анализа кода (опционально)
   - Скачать: https://x64dbg.com/

## Безопасность

⚠️ **ВАЖНО**: 
- Используйте только на локальных серверах или серверах без VAC
- Не используйте на официальных серверах Valve
- Создайте резервную копию игры перед экспериментами

## Процесс поиска смещений

### 1. Подготовка

1. Запустите Counter-Strike 1.6 (Steam версия)
2. Подключитесь к локальному серверу или создайте его
3. Запустите Cheat Engine
4. Выберите процесс `hl.exe` в Cheat Engine

### 2. Поиск данных игроков

#### 2.1 Поиск имени игрока

1. В CS 1.6 откройте консоль и введите `name YourTestName`
2. В Cheat Engine:
   - Value Type: String
   - Введите "YourTestName" в поле Value
   - Нажмите "First Scan"
3. Измените имя в игре на другое, например "NewName"
4. В Cheat Engine введите новое имя и нажмите "Next Scan"
5. Повторяйте, пока не найдете 1-2 адреса
6. Запомните адрес - это будет база для структуры игрока

#### 2.2 Поиск фрагов (Kills)

1. Запомните текущее количество фрагов (например, 5)
2. В Cheat Engine:
   - Value Type: 4 Bytes
   - Введите количество фрагов
   - Нажмите "First Scan"
3. Убейте противника в игре
4. Введите новое количество фрагов и нажмите "Next Scan"
5. Повторяйте, пока не найдете нужный адрес
6. Вычислите смещение: `killsOffset = killsAddress - playerBaseAddress`

#### 2.3 Поиск смертей (Deaths)

Аналогично поиску фрагов:
1. Value Type: 4 Bytes
2. Ищите текущее количество смертей
3. Умрите в игре и обновите поиск
4. Вычислите смещение: `deathsOffset = deathsAddress - playerBaseAddress`

#### 2.4 Поиск денег (Money)

1. Посмотрите текущее количество денег ($800, $3500 и т.д.)
2. В Cheat Engine:
   - Value Type: 4 Bytes
   - Введите количество денег
   - Нажмите "First Scan"
3. Купите что-нибудь или дождитесь следующего раунда
4. Введите новое количество и нажмите "Next Scan"
5. Вычислите смещение: `moneyOffset = moneyAddress - playerBaseAddress`

#### 2.5 Поиск команды (Team)

1. Value Type: 4 Bytes
2. Значения обычно:
   - 0 = Spectator
   - 1 = Terrorist
   - 2 = Counter-Terrorist
3. Найдите адрес и вычислите смещение

#### 2.6 Поиск статуса жизни (IsAlive)

1. Value Type: Byte или 4 Bytes
2. Ищите значение 1 (живой) или 0 (мертвый)
3. Умрите и обновите поиск
4. Вычислите смещение

### 3. Поиск данных бомбы

#### 3.1 Поиск статуса установки бомбы

1. Играйте за террориста с C4
2. В Cheat Engine ищите:
   - Value Type: Byte
   - Value: 0 (бомба не установлена)
3. Установите бомбу
4. Next Scan с Value: 1
5. Запомните адрес: `bombPlantedAddress`

#### 3.2 Поиск таймера бомбы

1. Установите бомбу
2. В Cheat Engine ищите:
   - Value Type: Float
   - Value: примерно 45.0 (или текущее время на таймере)
3. Подождите пару секунд
4. Next Scan с уменьшенным значением (например, 43.0)
5. Повторяйте до нахождения адреса
6. Вычислите смещение: `bombTimerOffset = bombTimerAddress - bombBaseAddress`

### 4. Использование найденных адресов

После нахождения всех адресов:

1. Определите базовый адрес модуля (обычно `hl.exe` или `hw.dll`)
2. Вычислите относительные смещения:
   ```
   offset = foundAddress - moduleBaseAddress
   ```

3. Обновите файл `src/game_data_capture.cpp`, метод `initializeOffsets()`:

```cpp
bool GameDataCapture::initializeOffsets() {
    uintptr_t baseAddr = memoryReader_->getModuleBase("hl.exe");
    
    // Ваши найденные смещения
    offsets_.playerListBase = baseAddr + 0x12345678;  // Замените на реальное значение
    offsets_.bombBase = baseAddr + 0x23456789;        // Замените на реальное значение
    
    // Смещения внутри структур
    offsets_.playerNameOffset = 0x04;      // Замените
    offsets_.playerKillsOffset = 0x40;     // Замените
    offsets_.playerDeathsOffset = 0x44;    // Замените
    offsets_.playerAssistsOffset = 0x48;   // Замените
    offsets_.playerMoneyOffset = 0x4C;     // Замените
    offsets_.playerTeamOffset = 0x50;      // Замените
    offsets_.playerAliveOffset = 0x54;     // Замените
    
    offsets_.bombPlantedOffset = 0x00;     // Замените
    offsets_.bombTimerOffset = 0x04;       // Замените
    offsets_.bombDefusedOffset = 0x08;     // Замените
    
    return true;
}
```

## Поиск через Pointer Scan (продвинутый метод)

Для поиска стабильных указателей:

1. Найдите нужный адрес как описано выше
2. В Cheat Engine: правый клик на адресе → "Pointer scan for this address"
3. Сохраните результаты сканирования
4. Перезапустите игру
5. Откройте сохраненное сканирование
6. "Rescan memory" - останутся только стабильные пути указателей
7. Используйте базовый адрес + смещения из пути указателя

## Автоматизация через скрипты Cheat Engine

Пример скрипта для быстрого поиска:

```lua
-- Cheat Engine Lua Script
local baseAddress = getAddress("hl.exe")

-- Поиск имени игрока
local nameResults = AOBScan("Player1") -- Замените на ваше имя

-- Вывод результатов
for i=0, nameResults.Count-1 do
    local addr = nameResults[i]
    print(string.format("Found at: 0x%X, Offset: 0x%X", addr, addr - baseAddress))
end
```

## Проверка найденных смещений

После обновления кода:

1. Пересоберите DLL
2. Запустите тестовый сервер WebSocket
3. Инжектируйте DLL в CS 1.6
4. Проверьте логи на наличие корректных данных
5. Убедитесь, что данные обновляются в реальном времени

## Типичные проблемы

### Адреса меняются при перезапуске
- Используйте pointer scan для поиска стабильных путей
- Ищите базу модуля + статическое смещение

### Данные читаются неправильно
- Проверьте размер типа данных (byte, int, float)
- Убедитесь, что смещения правильные
- Проверьте, что адрес валидный

### Структура игрока сложная
- CS 1.6 может использовать массив структур игроков
- Определите размер структуры (обычно 0x200-0x400 байт)
- Итерируйте по массиву: `playerAddr = baseAddr + (index * structSize)`

## Альтернативный подход: Сигнатуры паттернов

Вместо статических адресов используйте сигнатуры байт-кода:

```cpp
// Пример паттерна для функции обновления фрагов
std::vector<uint8_t> killsPattern = {
    0x55, 0x8B, 0xEC, 0x83, 0xEC, 0x10,  // push ebp; mov ebp, esp; sub esp, 10
    0x89, 0x4D, 0xF0                      // mov [ebp-10], ecx
};
std::string mask = "xxxxxxxxx";

uintptr_t funcAddr = memoryReader_->findPattern(killsPattern, mask, baseAddr, 0x100000);
```

## Документирование

Создайте файл `offsets_version.txt` с информацией:

```
CS 1.6 Version: Build 8684 (Steam)
Date Found: 2024-12-26
Game Hash: [MD5 hash of hl.exe]

Player Structure (Base: hl.exe + 0x12345678):
  +0x04: Name (char[32])
  +0x40: Kills (int32)
  +0x44: Deaths (int32)
  +0x48: Assists (int32)
  +0x4C: Money (int32)
  +0x50: Team (int32)
  +0x54: IsAlive (bool)

Bomb Structure (Base: hw.dll + 0x23456789):
  +0x00: Planted (bool)
  +0x04: Timer (float)
  +0x08: Defused (bool)
```

## Полезные ссылки

- CS 1.6 SDK: https://github.com/ValveSoftware/halflife
- HLSDK Documentation: https://developer.valvesoftware.com/
- Cheat Engine Tutorials: https://wiki.cheatengine.org/
- Pattern Scanning Guide: https://guidedhacking.com/threads/pattern-scanning.14112/

## Заключение

Поиск смещений - это итеративный процесс, требующий терпения. Сохраняйте все найденные адреса и регулярно проверяйте их актуальность после обновлений игры.

Удачи в поиске! 🎮
